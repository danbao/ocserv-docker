#!/usr/bin/expect

set timeout 2

set user [list user1 password1 user2 password2]

foreach {i j} $user {
        spawn ocpasswd -c /etc/ocserv/ocpasswd $i
        expect "*password*"
        send "$j\n"
        expect "*password*"
        send "$j\n"
        expect eof
}

exit

